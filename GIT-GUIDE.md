# 📚 Git Guide - Как правильно работать с проектом

## 🎯 Текущая ситуация

У тебя есть **ДВЕ версии** проекта:

### На GitHub (удаленный репозиторий):
```
❌ Ветка main - НЕТ (не создана)
✅ Ветка claude/review-code-... - СТАРАЯ версия
```

### Локально (на этом сервере):
```
✅ Ветка main - НОВАЯ версия с:
   - Рефакторингом кода (-40% размера)
   - Docker поддержкой
   - Модульной структурой
   - Переиспользуемыми компонентами
```

---

## ✅ Что нужно сделать ПЕРЕД git clone

### Шаг 1: Запушить main на GitHub

**Проблема:** Защита веток не дает пушить напрямую в main.

**Решения:**

#### Вариант А: Запушить с твоего компьютера
```bash
# Склонируй репозиторий на свой комп
git clone https://github.com/Nellumbo/pm-tool.git
cd pm-tool

# Создай ветку main
git checkout -b main

# Скопируй файлы отсюда в эту папку (или используй git pull с этого сервера)

# Запуши
git push -u origin main
```

#### Вариант Б: Создать Pull Request
```bash
# На GitHub:
# 1. Открой репозиторий
# 2. Перейди на ветку claude/review-code-...
# 3. Создай Pull Request в main
# 4. Смержи PR
```

#### Вариант В: Настроить main как default ветку
```bash
# На GitHub:
# Settings → Branches → Default branch → Выбери main
```

---

## 📥 После того как main будет на GitHub

### Git Clone скачает правильную версию:

```bash
git clone https://github.com/Nellumbo/pm-tool.git
cd pm-tool
```

Это скачает ветку **main** (если она дефолтная) со всеми улучшениями!

---

## 🤔 Какую ветку скачивает git clone?

### Git clone скачивает:
1. **Дефолтную ветку** репозитория (обычно `main` или `master`)
2. Все остальные ветки тоже, но не переключает на них

### Проверить дефолтную ветку:
- Открой https://github.com/Nellumbo/pm-tool
- Какая ветка показывается первой - та и дефолтная

### Изменить дефолтную ветку:
- `Settings` → `Branches` → `Default branch` → Выбери `main`

---

## 🌿 Работа с ветками после клонирования

### После git clone:
```bash
# Ты будешь на дефолтной ветке
git branch
# * main

# Посмотреть все ветки (включая удаленные)
git branch -a
# * main
#   remotes/origin/main
#   remotes/origin/claude/...

# Переключиться на другую ветку
git checkout claude/review-code-...

# Вернуться на main
git checkout main
```

---

## 🚀 Рекомендуемый Workflow

### 1. Первый раз:
```bash
# Склонируй
git clone https://github.com/Nellumbo/pm-tool.git
cd pm-tool

# Убедись что ты на main
git branch
# * main

# Установи и запусти
docker-compose up -d
# ИЛИ
npm run install-all && npm run dev
```

### 2. Обновление проекта:
```bash
# Забери новые изменения
git pull origin main

# Если используешь Docker
docker-compose up -d --build

# Если локально
npm run install-all
npm run dev
```

### 3. Внесение своих изменений:
```bash
# Создай свою ветку
git checkout -b feature/my-feature

# Делай изменения...

# Закоммить
git add .
git commit -m "Описание изменений"

# Запушить
git push -u origin feature/my-feature

# На GitHub создай Pull Request в main
```

---

## 📦 Альтернатива: Скачать ZIP

Если с git сложно, можно **скачать ZIP** прямо с GitHub:

1. Открой https://github.com/Nellumbo/pm-tool
2. Нажми зеленую кнопку **Code**
3. Выбери **Download ZIP**
4. Распакуй и запусти:
   ```bash
   cd pm-tool-main
   docker-compose up -d
   ```

⚠️ **Минус:** Нужно будет скачивать заново для обновлений.

---

## 🎓 Git терминология для новичков

| Термин | Что это |
|--------|---------|
| **Repository (repo)** | Проект/папка с кодом |
| **Clone** | Скачать копию репозитория |
| **Branch** | Версия кода (как параллельная вселенная) |
| **main/master** | Главная ветка с готовым кодом |
| **Commit** | Сохранение изменений |
| **Push** | Отправить на GitHub |
| **Pull** | Скачать с GitHub |
| **Pull Request (PR)** | Предложить изменения |
| **Merge** | Объединить ветки |
| **Remote** | Удаленный репозиторий (GitHub) |
| **Origin** | Имя удаленного репозитория |

---

## 🆘 Частые вопросы

### Что если я склонирую не ту ветку?
```bash
# Переключись на нужную
git checkout main
```

### Как посмотреть какая ветка активна?
```bash
git branch
# Ветка со звездочкой (*) активна
```

### Как обновить код после git pull?
```bash
# Docker
docker-compose up -d --build

# Локально
npm run install-all
```

### Как удалить проект и начать заново?
```bash
cd ..
rm -rf pm-tool
git clone https://github.com/Nellumbo/pm-tool.git
```

---

## ✅ Быстрая шпаргалка

```bash
# Склонировать проект
git clone https://github.com/Nellumbo/pm-tool.git

# Войти в папку
cd pm-tool

# Посмотреть ветки
git branch -a

# Переключить ветку
git checkout main

# Обновить код
git pull origin main

# Запустить (Docker)
docker-compose up -d

# Запустить (локально)
npm run install-all
npm run dev
```

---

**Теперь ты знаешь как работать с Git! 🎉**
