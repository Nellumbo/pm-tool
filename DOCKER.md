# 🐳 Docker - Запуск в 1 команду

## ⚡ Быстрый старт

### Установка и запуск:

```bash
# 1. Клонируй репозиторий
git clone https://github.com/Nellumbo/pm-tool.git
cd pm-tool

# 2. Запусти в 1 команду! 🚀
docker-compose up -d
```

**Готово!** Приложение запущено на http://localhost:5000

---

## 📋 Требования

- **Docker Desktop** ([Скачать](https://www.docker.com/products/docker-desktop))
  - Windows: Docker Desktop for Windows
  - Mac: Docker Desktop for Mac
  - Linux: Docker Engine + Docker Compose

Проверь установку:
```bash
docker --version
docker-compose --version
```

---

## 🎮 Команды управления

### Запуск приложения
```bash
# Запустить (в фоновом режиме)
docker-compose up -d

# Запустить с логами (увидишь что происходит)
docker-compose up

# Остановить
docker-compose down

# Перезапустить
docker-compose restart
```

### Просмотр логов
```bash
# Все логи
docker-compose logs

# Следить за логами в реальном времени
docker-compose logs -f

# Последние 100 строк
docker-compose logs --tail=100
```

### Управление
```bash
# Статус контейнера
docker-compose ps

# Остановить и удалить все (включая данные)
docker-compose down -v

# Пересобрать и запустить заново
docker-compose up -d --build
```

---

## 🌐 Доступ

### Локально
- **URL:** http://localhost:5000
- **API:** http://localhost:5000/api

### По сети
Узнай свой IP:
```bash
# Windows
ipconfig

# Mac/Linux
ifconfig
```

Доступ с других устройств: `http://[ТВОЙ_IP]:5000`

---

## 🔐 Демо-аккаунты

| Роль | Email | Пароль |
|------|-------|--------|
| Администратор | admin@example.com | admin123 |
| Менеджер | manager@example.com | manager123 |
| Разработчик | developer@example.com | dev123 |

---

## 💾 База данных

База данных SQLite хранится в Docker volume и **НЕ удаляется** при остановке контейнера.

### Сброс базы данных
```bash
# Остановить и удалить данные
docker-compose down -v

# Запустить заново (база пересоздастся)
docker-compose up -d
```

### Бэкап базы данных
```bash
# Скопировать БД из контейнера
docker cp pm-tool-app:/app/server/pm-tool.db ./backup.db

# Восстановить БД в контейнер
docker cp ./backup.db pm-tool-app:/app/server/pm-tool.db
docker-compose restart
```

---

## 🔧 Настройка

### Изменить порт
Отредактируй `docker-compose.yml`:
```yaml
ports:
  - "8080:5000"  # Теперь доступ на порту 8080
```

### Изменить переменные окружения
Отредактируй `docker-compose.yml`:
```yaml
environment:
  - PORT=5000
  - JWT_SECRET=твой-секретный-ключ
```

---

## 🐛 Решение проблем

### Контейнер не запускается
```bash
# Смотри логи
docker-compose logs

# Пересобери образ
docker-compose up -d --build
```

### Порт занят
```bash
# Измени порт в docker-compose.yml
ports:
  - "5001:5000"
```

### Приложение не отвечает
```bash
# Проверь статус
docker-compose ps

# Перезапусти
docker-compose restart
```

### Очистить всё и начать с нуля
```bash
# Удалить контейнер, образ и данные
docker-compose down -v
docker rmi pm-tool-pm-tool

# Запустить заново
docker-compose up -d --build
```

---

## 📊 Мониторинг

### Использование ресурсов
```bash
# Статистика в реальном времени
docker stats pm-tool-app
```

### Проверка здоровья
```bash
# Healthcheck автоматически проверяет /api/stats каждые 30 секунд
docker inspect pm-tool-app | grep -A 10 Health
```

---

## 🚀 Production деплой

### С кастомным JWT секретом
Создай файл `.env`:
```env
JWT_SECRET=очень-секретный-ключ-минимум-32-символа
PORT=5000
```

Запусти:
```bash
docker-compose --env-file .env up -d
```

### За Nginx reverse proxy
```nginx
server {
    listen 80;
    server_name yourdomain.com;

    location / {
        proxy_pass http://localhost:5000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

---

## 📦 Обновление приложения

```bash
# 1. Забери новый код
git pull

# 2. Пересобери и запусти
docker-compose up -d --build
```

---

## ✅ Одна команда для всего!

**Полная установка и запуск:**
```bash
git clone https://github.com/Nellumbo/pm-tool.git && \
cd pm-tool && \
docker-compose up -d && \
echo "✅ PM Tool запущен на http://localhost:5000"
```

**Остановка:**
```bash
docker-compose down
```

**Полная очистка:**
```bash
docker-compose down -v && \
docker rmi pm-tool-pm-tool
```

---

## 🎯 Преимущества Docker

✅ **Одна команда** - запуск без установки зависимостей
✅ **Изолированно** - не влияет на систему
✅ **Везде одинаково** - работает на Windows/Mac/Linux
✅ **Легко обновлять** - git pull + docker-compose up --build
✅ **Легко удалить** - docker-compose down

---

**Вот и всё! Одна команда и всё работает! 🎉**
