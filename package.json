{
  "name": "project-management-tool",
  "version": "1.0.0",
  "description": "Инструмент управления проектами на русском языке",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "build": "cd client && npm run build",
    "start": "cd server && npm start",
    "setup": "npm run install-all && npm run copy-env",
    "copy-env": "cp server/env.example server/.env && cp client/env.example client/.env",
    "clean": "rm -rf node_modules server/node_modules client/node_modules client/build"
  },
  "keywords": ["project-management", "russian", "react", "nodejs"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
